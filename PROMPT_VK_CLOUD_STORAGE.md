# Промт для интеграции VK облачных сохранений

Скопируйте и отправьте этот промт AI ассистенту для любой игры:

---

## Промт:

```
Интегрируй VK Bridge облачные сохранения для синхронизации прогресса между устройствами.

Требования:
1. Добавь VK Bridge SDK в index.html: <script src="https://unpkg.com/@vkontakte/vk-bridge/dist/browser.min.js"></script>

2. Инициализируй VK Bridge только для VK платформы (не трогай другие платформы)

3. Используй VK Bridge для сохранений:
   - VKWebAppStorageSet - для сохранения (по ключам, значения в строках)
   - VKWebAppStorageGet - для загрузки (массив ключей)

4. Сохраняй эти данные игрока:
   - Монеты/очки
   - Уровень прогресса
   - Купленные предметы/улучшения
   - Любые другие важные данные

5. ВАЖНО: При сохранении ВСЕГДА сначала загружай текущие данные из облака, потом обновляй нужное поле:
   ```javascript
   const currentData = await loadPlayerData();
   await savePlayerData({ ...currentData, coins: newCoins });
   ```

6. Добавь подробное логирование для отладки (console.log с эмодзи ✅ 💾 📥)

7. Не трогай рекламу и другие функции - только сохранения!

8. Сделай fallback на localStorage для dev режима

Проверь что всё работает и не сломана логика игры.
```

---

## Короткая версия (минимум):

```
Добавь VK Bridge облачные сохранения:
1. Подключи VK Bridge SDK в HTML
2. Инициализируй для платформы VK
3. Используй VKWebAppStorageSet/Get для сохранения прогресса
4. При сохранении ВСЕГДА загружай текущие данные, потом обновляй
5. Добавь логи для отладки
6. Не трогай рекламу и другие функции
```

---

## Что проверить после интеграции:

1. Откройте консоль браузера (F12)
2. Должны быть логи:
   - `✅ VK Bridge инициализирован`
   - `💾 Сохранение данных через VK Bridge...`
   - `📥 Загрузка данных через VK Bridge...`
3. Пройдите уровень на ПК
4. Откройте игру на телефоне - прогресс должен синхронизироваться

---

## Пример кода для справки:

### Инициализация:
```javascript
if (bridge.platform.id === 'vk' && typeof window.vkBridge !== 'undefined') {
  vkBridge = window.vkBridge;
  await vkBridge.send('VKWebAppInit');
}
```

### Сохранение:
```javascript
await vkBridge.send('VKWebAppStorageSet', {
  key: 'gameCoins',
  value: String(coins)
});
```

### Загрузка:
```javascript
const result = await vkBridge.send('VKWebAppStorageGet', {
  keys: ['gameCoins', 'gameLevel']
});
// result.keys = [{ key: 'gameCoins', value: '100' }, ...]
```

### Правильное обновление данных:
```javascript
// ✅ ПРАВИЛЬНО
const currentData = await loadPlayerData();
await savePlayerData({ ...currentData, coins: newCoins });

// ❌ НЕПРАВИЛЬНО (перезапишет все данные!)
await savePlayerData({ coins: newCoins });
```
